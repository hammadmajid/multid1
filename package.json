{
  "name": "multid1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest run",
    "test:remote": "bun test/remote-prod.ts",
    "db:generate": "drizzle-kit generate --config=drizzle.users.config.ts && drizzle-kit generate --config=drizzle.cart.config.ts && drizzle-kit generate --config=drizzle.catalog.config.ts && drizzle-kit generate --config=drizzle.orders.config.ts && drizzle-kit generate --config=drizzle.reviews.config.ts",
    "db:migrate:remote": "wrangler d1 migrations apply multid1-users --remote && wrangler d1 migrations apply multid1-cart --remote && wrangler d1 migrations apply multid1-catalog --remote && wrangler d1 migrations apply multid1-orders --remote && wrangler d1 migrations apply multid1-reviews --remote"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.32",
    "ulid": "^3.0.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^5.20260726.1",
    "drizzle-kit": "^0.31.10",
    "vitest": "^4.1.10",
    "wrangler": "^4.110.0"
  }
}
